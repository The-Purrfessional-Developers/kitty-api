{
  "name": "kitty-api",
  "scripts": {
    "server": "nodemon server",
    "migrate": "export NODE_ENV=migration; node ./migration/migration",
    "production": "export NODE_ENV=production; node server",
    "genKey": "node security/genKey.js 20 10",
    "example": "export NODE_ENV=examples; node ./examples/request.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "config": "^3.2.6",
    "express": "^4.17.1",
    "express-validator": "^6.4.0",
    "https": "^1.0.0",
    "mongoose": "^5.9.2"
  },
  "devDependencies": {
    "node-fetch": "^2.6.0",
    "nodemon": "^2.0.3"
  }
}
